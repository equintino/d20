function scriptBreed() {
    // $(breed).find("button").on("click", (e) => {
    //     let btnName = e.target.innerText
    //     if(btnName === "Limpar") {
    //         $(topHeader).find("[data-page='breed/add']").trigger("click")
    //     }
    //     if(btnName === "Salvar") {
    //         let formData = new FormData($(breed).find("form")[0])
    //         saveData("breed/save", formData)
    //     }
    // })
}
