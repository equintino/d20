function scriptCategory() {
    // $(category).find("button").on("click", (e) => {
    //     let btnName = e.target.innerText
    //     if(btnName === "Limpar") {
    //         $(topHeader).find("[data-page='category/add']").trigger("click")
    //     }
    //     if(btnName === "Salvar") {
    //         let formData = new FormData($(breed).find("form")[0])
    //         saveData("category/save", formData)
    //     }
    // })
}
